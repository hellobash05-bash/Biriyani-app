services:
  - type: web
    name: biriyani-backend
    env: node
    rootDir: backend
    buildCommand: npm install --production
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: PORT
        value: "5000"
      - key: MONGODB_URI
        sync: false
